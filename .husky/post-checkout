#!/bin/sh
# Wrapper: calls oc-agents post-checkout hook
HOOKS_DIR="C:/Users/andrseli/.config/opencode/hooks"
if [ -f "$HOOKS_DIR/post-checkout" ]; then
  sh "$HOOKS_DIR/post-checkout" "$@" || exit $?
fi
